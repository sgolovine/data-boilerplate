# Data Directory

Store research data and generated datasets here.

The starter SQLite database is `research.sqlite`. It begins with neutral
findings and sources tables so agents have a stable place to write research
output before the project needs a more specialized schema.
